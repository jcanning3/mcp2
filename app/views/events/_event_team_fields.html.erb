<div class="nested-fields">
  <%= f.association :team, label_method: :nickname, collection: Team.order(:nickname) %>

  <%= f.check_box :_destroy %>

  <%= link_to_remove_association "remove link", f, class: 'btn btn-danger btn-sm' %>
</div>

<br>
