<p id="notice"><%= notice %></p>

<h1>Pieces</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Composer</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pieces.includes(:composer).order("composers.nickname", :name).each do |piece| %>
      <tr>
        <td><%= piece.composer.nickname %> • <%= piece.full_title %></td>
        <td><%= piece.composer.name %></td>
        <td><%= link_to 'Show', piece, class: 'btn btn-warning btn-sm' %></td>
        <td><%= link_to 'Edit', edit_piece_path(piece), class: 'btn btn-warning btn-sm' %></td>
	<!td><!%= link_to 'Destroy', piece, method: :delete, data: { confirm: 'Are you sure?' } %><!/td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Piece', new_piece_path, class: 'btn btn-primary btn-xl' %>
